---
title: Installation
description: Install specs.md and set up your development environment
---

## Prerequisites

- Node.js 18 or higher
- An AI coding tool (Claude Code, Cursor, GitHub Copilot, or Google Antigravity)

## Install specs.md

<Note>
  Always use npx to get the latest version. Do not install globally with npm.
</Note>

```bash
npx specsmd@latest install
```

The installer automatically detects your AI coding tools and sets up:

<CardGroup cols={3}>
  <Card title="Agent Definitions" icon="robot">
    Specialized agents for each phase of development
  </Card>
  <Card title="Memory Bank" icon="database">
    Persistent context storage structure
  </Card>
  <Card title="Slash Commands" icon="terminal">
    Easy agent invocation commands
  </Card>
</CardGroup>

## What Gets Installed

After installation, you'll see the following structure:

```
.specsmd/
├── manifest.yaml              # Installation manifest
└── aidlc/                     # AI-DLC flow
    ├── agents/                # Agent definitions
    ├── skills/                # Agent capabilities
    ├── templates/             # Artifact templates
    │   └── standards/         # Standards facilitation guides
    └── memory-bank.yaml       # Memory bank schema
```

## Tool-Specific Setup

<Tabs>
  <Tab title="Claude Code">
    Slash commands are installed in `.claude/commands/`:
    - `/specsmd-master-agent`
    - `/specsmd-inception-agent`
    - `/specsmd-construction-agent`
    - `/specsmd-operations-agent`
  </Tab>
  <Tab title="Cursor">
    Rules are installed in `.cursor/rules/` using `.mdc` format.
  </Tab>
  <Tab title="GitHub Copilot">
    Agents are installed in `.github/agents/` using `.agent.md` format.
  </Tab>
  <Tab title="Google Antigravity">
    Agents are installed in `.agent/workflows/`.
  </Tab>
</Tabs>

## VS Code Extension (Optional)

Track your AI-DLC progress with our sidebar extension. Works with VS Code and compatible IDEs including Cursor, Windsurf, and Google Antigravity.

<img
  src="/images/extension-preview.png"
  alt="VS Code Extension Preview"
  style={{ borderRadius: '8px', marginBottom: '16px' }}
/>

<CardGroup cols={2}>
  <Card
    title="VS Code Marketplace"
    icon="microsoft"
    href="https://marketplace.visualstudio.com/items?itemName=fabriqaai.specsmd"
  >
    Install from the official marketplace
  </Card>
  <Card
    title="GitHub Releases"
    icon="github"
    href="https://github.com/fabriqaai/specs.md/releases"
  >
    Download VSIX for manual installation
  </Card>
</CardGroup>

### Features

- **Bolts Tab** - Track active bolts, stage progress, and up next queue
- **Specs Tab** - Browse intents, units, and stories hierarchically
- **Overview Tab** - View project metrics and suggested actions

<Note>
  The extension automatically activates when it detects a `memory-bank` or `.specsmd` folder in your workspace.
</Note>

## Verify Installation

After installation, verify everything is set up correctly:

```bash
# Check the manifest
cat .specsmd/manifest.yaml

# List installed agents
ls .specsmd/aidlc/agents/
```

## Next Steps

<Card
  title="Quick Start"
  icon="arrow-right"
  href="/getting-started/quick-start"
>
  Initialize your project and create your first Intent
</Card>
